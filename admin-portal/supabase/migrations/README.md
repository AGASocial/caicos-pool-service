# Migrations moved

All SQL migrations (including billing, legacy bootstrap, and Cadenza schema changes) now live in one place:

**[`/supabase/migrations/`](../../../supabase/migrations/)** (repository root)

Use the Supabase CLI from the **cadenza** repo root with that `supabase/` directory, or link this repo’s root `supabase` folder to your project.
