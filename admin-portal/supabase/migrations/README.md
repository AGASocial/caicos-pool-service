# Migrations moved

All SQL migrations (including billing, legacy bootstrap, and Caicos schema changes) now live in one place:

**[`/supabase/migrations/`](../../../supabase/migrations/)** (repository root)

Use the Supabase CLI from the **caicos** repo root with that `supabase/` directory, or link this repo’s root `supabase` folder to your project.
